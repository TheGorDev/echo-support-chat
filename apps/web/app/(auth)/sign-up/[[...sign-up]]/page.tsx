import { SignUpView } from '@/modules/auth/ui/views/sign-up-view'
import React from 'react'

/**
 * Renders the sign-up view for the authentication flow.
 *
 * @returns The component tree containing the `SignUpView` for the sign-up page.
 */
export default function Page() {
  return (
    <SignUpView />
  )
}